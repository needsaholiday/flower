# flower

Debug application for Benthos / RedpandaConnect

## Commands

`sudo docker run --rm -p 4195:4195 -v $(pwd)/sample-connect.yaml:/connect.yaml   docker.redpanda.com/redpandadata/connect:4.74.0 -c /connect.yaml`

`npm run dev`
